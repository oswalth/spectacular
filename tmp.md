You are AI expert with experience in building and designing AI assisted SDLC - starting from a raw idea to PDDs, PRDs, ARDs, ADRs, etc.  till the implemented and released feature (I might be wrong about the order, correct me if needed so that it is correct for a regular software product including design, planning, development, infra, testing ,release ,etc.). My goal is to build a reusable plugin that would help me automate such processes to build a software product of any scale with the help of AI - designing the app, its features, capabilities, planning and splitting work into separate epics, stories, tasks ,etc. My goals are:
1. A reusable plugin published that helps users to start and develop a product
2. Plugin has nice and clear documentation with all skills/commands/agents described. Their overall description, then individual description, example of usage and use cases ideally ordered in the correct order projects are usually worked (from project brief to implementation, testing and release). Documentation is always up to date with plugins files. Every update must trigger docs update to make sure it's relevant. Documentation is clear on the installation path, usage, update and so on. Documentaion is structured in a nice, readable way and is human friendly. 

I've already tried creating a tool like that (sibling repos speck and speculation), but these were failing due to the folllowing reasons:
1. Too complicated structure that never led to actual development
2. No linkage between workspace repo and code repos
3. Using plugins to bootstrap and foundate the project worked, but plugin was hallucinating, loosing some important notes, treating example project as a basis (plugin was highly referncing real name of the test project and was too specific about it)
4. Commands finished with no further suggested actions. User had to guess what to do next
5. speck was build in a way to create some commands, test them and if they are fine then create additional ones - that lead to the fact that plugin suggested executing commands that do not yet exist.
6. It was unclear how to start a project's PRDs and architecture - considering test project was API + WEB + IaC, and it was hard to understand where and when to make some architectural decisions. Feel like a dependncy graph or a roadmap might be helpful - either text or a diagram or even both that shows last N implemented bits, current stage, further actions we can do with the project (commands to run, justify why, etc.), this is not always a straight line, and could possible be a branching graph.

What I'd like to have:
0. Plugin works with claude code as for now
1. Plugin works with multiple repos. There is always at least one repo - project workspace - it holds product description, all features, PRDs, architecture, epics, stories, etc. There could also be so called implementation/code repos - repos that contain code for a specific part of the product - could be any (web, api, iac, data, mobile, e2e tests, etc.). Plugins commands could be run in any repo. There should be a biderctional link between worksapce repo and code repos - they should know about each other. By default they simply read it from a sibling directory. But there should be also a possibility to bootstrap the project and setup GH MCP that would allow both to read latest states of each repo (main branch). Workspace repo might read code repos to make some decision on what is the current architecture of the certain implementation or to assess what option would work best for a new feature given existing implementation (it should also try to understand what kind of repos it needs to read - for example if we add a new file type field it might need to undersand if infra has a s3 like storage already. but if it's simply change the color of the button, then it's only a relevant web repo change). Code repos would need to read workspace repos when implementing the features - for example, we start api implementation, it would need to read its task from workspace repo, current architecture (possibly worth ingesting this kind of stuff in task/story/epic itself during the planning) and anything else needed, implements the change, and it might also trigger a change to the workspace - for example if the architecture was updated (not sure if code repos should be doing this, may be when there is a decision made during prd,ard or task planning it's already in the workspace - have to think of this better).
3. Plugin should have a capability to evolve. For example, I use it in one of the repos, then I see plugin is doing somthing bad. I'd need to record this as a feedback (in a nice way - either a template I'd fill or a command I'll execute, provide my inputs, it might read current repo's files to get more context or any other way) and then hand it over to the plugin repo itself to read, ask user for input if needed, evolve, update, push the change and finally suggest user to update/realod plugin
4. Plugin should allow users to start a project that would bootsrap it with main files needed like docs, README, claude.md etc
5. Each plugin's command should always suggest what to do next (at least one option), specifying what to do next (ideally run another plugin command) specifying steps, command, input, arguments, whatever needed with a justificaion why it thinks its the best thing to do right now. No command would ever leave a user to themselve not knowing what to do and guessing what are the nexet steps. 
6. Plugin should have clear commands. I'll list some commands here - do no treat list as a non negotiable source of truth, these are only my suggestion. Ideally, I'd keep list of commands/agents short and would only add new ones when needed. When I'm saying command it could be any piece of work claude uses - skill/command/agent/ whatever fits best. But still we'd need a set of commadnds to start:
6.1. Some init command that starts the project, createas workspace repo, asks user about product idea, acting as a BA to gather all product details, constraints, ideas, goals, etc. Its an interactive process of communiacation between a user and a model which goal is to clarify all the details needed about the product and that are needed to start designing something. As part of this step we might also create a naming convention for the project - you may read this as an example for ERP/CTRM for agro traders - /Users/vladimircarpa/work/wardx/gdansk/context/conventions.md
6.2. A command to transform idea into set of PRDs (could be one PRD at a time, but user should know the suggested PRDs list as a result of running the first command and the order to genreate them into). I'm
6.3. A command that does further steps - Not entirely sure what should come next in regular SDLC in AI assisted projects. We'll discuss those points

All points from my goals, except point 0, could be challenged or suggested an improvvement from you. I'm open for any discussion

Your goal is to help me settle my idea, structurize it and only then suggest implementation. Areas I'd like to focus on (you decide the correct order):
1. Clarifying processess. Since I'm not quite familiar with all steps SDLC involves, we'd need to clarify what steps might exist, what I'll actually need (some could be out of the scope because it's only used by large teams or enterprises - for example, I don't need to define security policies at this stage).
2. Clarifying commands/skills/agents/etc, we might need, listing them, describing and providing their use cases, limitations and possible further actions each might be followed by
3. Discuss pain points of the failed attempts I've had, try to resolve them and avoid having those with this newer version. I'd like to avoid rebuilding failed attempt for the third time again - I'm happy to start from the scratch, considering all failures. My goal is quality, not reusing those two failed plugins.

Ways of working with me:
1. Always ask for more clarifications, never assume
2. Do not overcomplicate things
3. Use /karpathy-guidelines skill
4. Think of a context rot in advance. Try to plan our conversation in a way it's splittable into sessions. Each new session should have all the context needed to not loose any quality. You think of a way of making this useful. Crete a local context file, generate a resuable prompt I'll feed with dynamic file, give me a follow up prompt to copy paste, no matter, you decide ewhat's the best way. 
5. Use this directory as plugin's repo