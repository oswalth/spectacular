# <product-name> — design language

One page, distilled from the imported design code. Rides in every implement
capsule for UI tasks — rules only; values live in `tokens.json`.

## Principles

<!-- 3–5 lines: the product's visual character in words. -->

## Typography roles

<!-- Which font/scale token serves UI chrome, reading content, code; when
each is used and never used. -->

## Color usage

<!-- The semantic token for each purpose; hard rules (e.g. no raw hex in
code, ever — tokens only). -->

## Components

<!-- Anatomy rules that span components: radii, borders, elevation, density,
recurring motifs. -->

## Motion & interaction

<!-- Durations, easing, hover/press conventions — by token reference. -->

## Do / Don't

<!-- The short list that prevents drift. -->
